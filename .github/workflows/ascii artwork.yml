name: Generate ASCII

on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        -  name: checkout repo
           uses: actions/checkout@v4
        
        -  name: list repo files
           run: ls -lrta
           
        -  name: Execute shell
           run: | 
             chmod +x ./script.sh
             ./script.sh

       