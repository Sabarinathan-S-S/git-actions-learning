name: Workflow CI 

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        -  name: checkout repo
           uses: actions/checkout@v4

        -  name: Welcome!
           run: echo "My first GA job"

        -  name: List files
           run: ls

        -  name: Read file
           run: cat README.md
